[project]
name = "lib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.3",
]

[dependency-groups]
dev = [
    "mypy>=1.15.0",
]
